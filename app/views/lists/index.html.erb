<div class='container'>
  <h1> Lists </h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list), class: 'text-decoration-none text-primary' %></li>
    <% end %>
    <ul>
    </div>
    <%= link_to "New", new_list_path, class: 'btn btn-primary mx-3' %>
